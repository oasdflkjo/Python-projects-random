To install Cython you have 3 options: 
1. Run pip install Cython or 
2. Goto cython.org., download it and run python setup.py install or 
3. Run pip install Cython --install-option="--no-cython-compile" 

not shure on what compiled function into c